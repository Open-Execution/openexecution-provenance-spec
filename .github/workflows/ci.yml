name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions: read-all

jobs:
  sdk-js-check:
    name: JS SDK Checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          node-version: '20'
      - name: Verify SDK loads
        run: node -e "require('./sdk/js/src/verify.js')"
      - name: Run unit tests
        run: node sdk/js/test/verify.test.js
      - name: Verify types exist
        run: test -f sdk/js/src/types.d.ts

  sdk-python-check:
    name: Python SDK Checks
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.8', '3.10', '3.12']
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install SDK
        run: cd sdk/python && pip install -e .
      - name: Verify import
        run: python -c "from openexecution_verify import OpenExecutionVerifier; print('SDK loaded successfully')"
      - name: Run unit tests
        run: cd sdk/python && python test_verify.py

  schema-validate:
    name: Schema Validation
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: oe_test
          POSTGRES_PASSWORD: test_password
          POSTGRES_DB: spec_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - name: Enable uuid-ossp extension
        run: PGPASSWORD=test_password psql -h localhost -U oe_test -d spec_test -c 'CREATE EXTENSION IF NOT EXISTS "uuid-ossp";'
      - name: Validate schema-spec.sql
        run: PGPASSWORD=test_password psql -h localhost -U oe_test -d spec_test -f schema/schema-spec.sql
